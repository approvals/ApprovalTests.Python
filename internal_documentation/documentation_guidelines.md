# Document for users

Documentation is split into 4 areas (how_to, explantation, reference, tutorial)
Most pages will be in the how_to section.

## Code Snippets
we use md_snippets to insert code from tests (sometime production). This keeps the code up to date. 
MdSnippets runs automatically with the CI so you don't have to worry about running it.
