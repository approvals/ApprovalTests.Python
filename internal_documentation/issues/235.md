# Issue 235 - Beyond Compare Reporter

## Overview

``` mermaid
flowchart TD
    start[investigate] --> diffreporter[Investigate how the current default diffreporter works]
    diffreporter --> json[Figure out relationship to reporters.json]
    json --> bc[Figure out how the current BeyondCompare reporter works]
    bc --> fix[Fix it]
```

1.  [ ] reporters.json should be empty(ish)
2.  [ ] Reporters.md should exist in reference
3.  [ ] WindowsReporters, MacReporters, LinuxReporters should exist in reporters module
4.  [ ] things in reporters.json should move to classes 

- name that works across OS
- TEXT and/or IMAGE
- path to the executable
- (optional) arguments with placeholders
- can substitute ProgramFiles on windows